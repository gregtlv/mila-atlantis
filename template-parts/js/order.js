// Get Offer

function getOffer (dirUrl) {
	// body... 
	var dirUrl;
    document.write(dirUrl);

}


// Offers

function showOrderForm () {
  // body... 


  var OffersList = document.getElementById('OffersList');

  OffersList.style.opacity = '0';

  // $('#main').load(tempParts + 'ajax/order_form.php');
  

  // OffersList.style.width  = '10%';


  // $.get('http://localhost:8888/mila-wp/order/').then(function(responseData) {
  // //responseData is the contents of the other page. Do whatever you want with it.
  // $('#mainOffers').append(responseData);
  // });

}